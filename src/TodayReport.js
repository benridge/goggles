var React = require('react');
var LocationReportRow = require('./component/LocationReportRow');

module.exports = React.createClass({
  render: function() {
    return (
      <LocationReportRow />
    );
  }
});
